def greet(name) -> str:
    return f"{name} Hello111!"

if __name__ == '__main__': # Точка входа (желательно только одна). Магическая = принадлежит питону
    print('lolol haha')       # Запускается только на прямую, не в импорте
